mixin checkbox(id, title)
	if attributes.mods
		-var mods = []
		each mod, i in attributes.mods
			-mods[i] = 'checkbox--' + mod

	.checkbox(class=mods)
		label.checkbox__label(for=id)
			input.checkbox__input(
				type='checkbox'
				id=id)
			span.checkbox__text!=title
		if block
			block